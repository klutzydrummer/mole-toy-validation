---
description: "Phase B' verification for a named component or all tracked components. /verify [component]"
---

First, run `python utils/verify.py status` and note which components exist and which are stale.

Then read `.claude/rules/run-full-verification.md` and execute the Phase B' protocol.

Target component: $ARGUMENTS
(If empty, verify all tracked components shown by the status output above.)
